{
    "tasks": [
        {
            "type": "shell",
            "label": "clang-4.0 build active file",
            "command": "/anaconda3/bin/clang-4.0",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/anaconda3/bin"
            }
        }
    ],
    "version": "2.0.0"
}